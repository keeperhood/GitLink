class GitInfo():
    def __init__(self, user, repo, branch, remote_path, remote_name, commit, project):
        self.user = user
        self.repo = repo
        self.branch = branch
        self.remote_path = remote_path
        self.remote_name = remote_name
        self.commit = commit
        self.project = project